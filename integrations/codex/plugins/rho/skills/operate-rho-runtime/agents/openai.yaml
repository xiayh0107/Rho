interface:
  display_name: "Operate Rho Runtime"
  short_description: "Operate R workspaces and runtime dependencies"
  default_prompt: "Use $operate-rho-runtime to connect this project, verify its managed dependencies, and inspect its authoritative scientific workspace."

policy:
  allow_implicit_invocation: true
